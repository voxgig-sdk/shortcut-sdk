# BasicMilestoneFlow

main: kit: flow: BasicMilestoneFlow:
{
  entity: "milestone",
  kind: "basic",
  name: "BasicMilestoneFlow",
  step: [
    {
      input: {
        ref: "milestone_ref01"
      }

      match: {
        category_id: "category01"
      }

      op: "create"
    }

    {
      op: "list",
      valid: [
        {
          apply: "ItemExists",
          def: {
            ref: "milestone_ref01"
          }
        }
      ]
    }

    {
      input: {
        ref: "milestone_ref01",
        srcdatavar: "milestone_ref01_data",
        suffix: "_up0",
        textfield: "app_url"
      }

      op: "update",
      spec: [
        {
          apply: "TextFieldMark",
          def: {
            mark: "Mark01-milestone_ref01"
          }
        }
      ]
    }

    {
      input: {
        ref: "milestone_ref01",
        srcdatavar: "milestone_ref01_data",
        suffix: "_dt0"
      }

      match: {
        id: "milestone01"
      }

      op: "load",
      valid: [
        {
          apply: "TextFieldMark",
          def: {
            mark: "Mark01-milestone_ref01"
          }
        }
      ]
    }

    {
      input: {
        ref: "milestone_ref01",
        suffix: "_rm0"
      }

      match: {
        id: "milestone01"
      }

      op: "remove"
    }

    {
      input: {
        suffix: "_rt0"
      }

      op: "list",
      valid: [
        {
          apply: "ItemNotExists",
          def: {
            ref: "milestone_ref01"
          }
        }
      ]
    }
  ],
  "key$": "BasicMilestoneFlow"
}